#include <stdio.h> // The library file that contains the code where the compiler reads from
// we will learn about libraries and we will call our own ones later

// int main() is a special function where the compiler knows to start
int main(){

printf("Hello Kadir"); // calls the printf function


return 0; // exits the program and gives the value 0 as output of the main function
}
